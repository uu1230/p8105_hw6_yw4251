---
title: "p8105_hw5_yw4251"
author: "You Wu"
date: 2023-11-12
output: github_document
---
# Problem 0
Load Necessary Packages.
```{r}
library(tidyverse)
library(purrr)
library(ggridges)
knitr::opts_chunk$set(
  fig.width = 8,
  fig.asp = .8,
  out.width = "90%"
)
```

# Problem 2
Load the dataset.
```{r}
weather_df = 
  rnoaa::meteo_pull_monitors(
    c("USW00094728"),
    var = c("PRCP", "TMIN", "TMAX"), 
    date_min = "2022-01-01",
    date_max = "2022-12-31") |>
  mutate(
    name = recode(id, USW00094728 = "CentralPark_NY"),
    tmin = tmin / 10,
    tmax = tmax / 10) |>
  select(name, id, everything())
weather_df
```

Finish the bootstrap.
```{r}

bootstrap_analysis=
  weather_df|>
  modelr::bootstrap(n = 5000)|>
  mutate(
    models = map(strap, \(df) lm(tmax ~ tmin + prcp,data=df)),
    result = map(models, broom::tidy),
    log_beta01_product=map(result, ~log(.x$estimate[1] * .x$estimate[2])),
    log_beta12_product=map(result, ~log(.x$estimate[2] * .x$estimate[3])),
    r.squared=map(models, ~broom::glance(.x)$r.squared)
    )|>
  select(-result)|>
  unnest(c(log_beta01_product,log_beta12_product, r.squared))
bootstrap_analysis
```

Estimate of R^2.
```{r}
quantile(bootstrap_analysis$r.squared,c(0.025, 0.975))
ggplot(bootstrap_analysis,aes(r.squared))+
  geom_density()+
  labs(title = "Distribution of R Square")
```

```{r}
quantile(bootstrap_analysis$log_beta01_product,c(0.025, 0.975))
ggplot(bootstrap_analysis,aes(log_beta_product))+
  geom_density()+
  labs(title = "Distribution of ln(β̂0 * β̂1)")
```

```{r}
quantile(bootstrap_analysis$log_beta12_product,c(0.025, 0.975),na.rm=TRUE)
ggplot(bootstrap_analysis,aes(log_beta_product))+
  geom_density()+
  labs(title = "Distribution of ln(β̂1 * β̂2)")
```

